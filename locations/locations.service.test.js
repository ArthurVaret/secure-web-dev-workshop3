const locationsService = require('./locations.service')
const locationsModel = require('./locations.model')
const jest = require(jest);

jest.mock('./locations.model')

describe('Should create a location', () => {
    it('return null', () => {


    })
    it('return the location', () => {


    })
} );


